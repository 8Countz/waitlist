import * as React from "react";

const IndexPage = () => {
  return (
    <main>
      <h1>Welcome home dancers</h1>
    </main>
  );
};

// Page's metadata will go into Head
export const Head = () => <title>8Countz</title>;

export default IndexPage;
